[phases.setup]
nixPkgs = ["python311", "libtiff", "zlib", "libjpeg", "libwebp", "freetype", "lcms2", "openjpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"